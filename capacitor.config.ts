import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.worldcup26.tracker',
  appName: 'World Cup 26',
  webDir: 'dist/worldcup26/browser',
  backgroundColor: '#07090d',
};

export default config;
